{
  "name": "phaser3-esbuild",
  "version": "0.0.1",
  "scripts": {
    "dev": "esbuild index.ts --serve=3000 --servedir=public --bundle --sourcemap --target=es6",
    "build": "esbuild index.ts --bundle --minify --target=es6 --outfile=public/index.js"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "esbuild": "^0.13.8"
  }
}
